:root {
  --ds-bg: #f3f5fb;
  --ds-surface: #ffffff;
  --ds-surface-2: #f8f9fd;
  --ds-border: #e4e7f0;
  --ds-text: #0f172a;
  --ds-muted: #64748b;
  --ds-primary: #5b6bff;
  --ds-accent: #20e0a5;
  --ds-shadow: 0 24px 80px rgba(15, 23, 42, 0.12);
  --ds-shadow-soft: 0 10px 35px rgba(15, 23, 42, 0.08);
  --ds-radius: 16px;
}

.layout {
  display: flex;
  min-height: 100vh;
  background:
    radial-gradient(circle at 10% 10%, rgba(91, 107, 255, 0.08), transparent 22%),
    radial-gradient(circle at 90% 8%, rgba(32, 224, 165, 0.08), transparent 22%),
    var(--ds-bg);
  color: var(--ds-text);
}

/* SIDEBAR */
.sidebar {
  width: 260px;
  background: linear-gradient(170deg, #0f172a 0%, #11182f 35%, #0f182e 100%);
  color: #e2e8f0;
  border-right: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 12px 0 35px rgba(15, 23, 42, 0.35);
  display: flex;
  flex-direction: column;
  gap: 1.2rem;
  padding: 1.6rem 1.2rem;
  position: sticky;
  top: 0;
  min-height: 100vh;
}
.sidebar-header {
  display: flex;
  align-items: center;
  gap: 0.85rem;
  font-weight: 800;
  font-size: 1.1rem;
}
.sidebar-header i {
  color: var(--ds-accent);
  background: linear-gradient(135deg, var(--ds-primary), var(--ds-accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.sidebar-nav {
  display: grid;
  gap: 0.4rem;
}
.nav-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.95rem 0.9rem;
  border-radius: 14px;
  color: #cbd5e1;
  font-weight: 700;
  background: rgba(255, 255, 255, 0.04);
  transition: transform 0.18s ease, background 0.18s ease, box-shadow 0.18s ease;
  border: 1px solid rgba(255, 255, 255, 0.04);
}
.nav-item i { width: 18px; text-align: center; color: var(--ds-accent); }
.nav-item:hover { background: rgba(255, 255, 255, 0.08); transform: translateX(2px); }
.nav-item.active {
  background: linear-gradient(135deg, rgba(91, 107, 255, 0.22), rgba(32, 224, 165, 0.22));
  border: 1px solid rgba(255, 255, 255, 0.12);
  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.06);
  color: #fff;
}
.sidebar-footer { margin-top: auto; }

/* MAIN */
.main {
  flex: 1;
  padding: clamp(1.4rem, 2vw, 2.3rem);
}
.topbar,
.top-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 1.2rem;
}
.topbar-user,
.top-bar-right {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  color: var(--ds-muted);
}
.topbar .sidebar-toggle,
.top-bar .sidebar-toggle {
  width: 44px;
  height: 44px;
  border-radius: 14px;
  border: 1px solid var(--ds-border);
  background: var(--ds-surface);
  color: var(--ds-text);
  display: grid;
  place-items: center;
  box-shadow: var(--ds-shadow-soft);
}
.content {
  display: flex;
  flex-direction: column;
  gap: 1.1rem;
}

/* SECTIONS & CARDS */
.section {
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: var(--ds-radius);
  padding: 1.4rem;
  box-shadow: var(--ds-shadow-soft);
}
.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 1rem;
}
.card {
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: var(--ds-radius);
  padding: 1.4rem;
  box-shadow: var(--ds-shadow-soft);
}
.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 1rem;
}
.card-header h2 { margin: 0; font-size: 1.15rem; }

.stats-grid,
.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1rem;
}
.stat-card {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 0.9rem;
  align-items: center;
  background: linear-gradient(135deg, rgba(91, 107, 255, 0.12), rgba(32, 224, 165, 0.1));
  border: 1px solid rgba(91, 107, 255, 0.16);
  border-radius: 15px;
  padding: 1.05rem 1.2rem;
  box-shadow: var(--ds-shadow-soft);
}
.stat-icon {
  width: 54px;
  height: 54px;
  border-radius: 14px;
  display: grid;
  place-items: center;
  background: #11182f;
  color: var(--ds-accent);
  font-size: 1.3rem;
  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.08);
}
.stat-content .stat-label { color: var(--ds-muted); font-size: 0.95rem; }
.stat-content .stat-value { font-size: 1.6rem; font-weight: 800; color: var(--ds-text); }

/* TABLES */
.table-responsive { width: 100%; overflow-x: auto; }
.table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0 0.65rem;
}
.table thead th {
  text-align: left;
  color: var(--ds-muted);
  font-weight: 700;
  padding: 0.9rem 1rem;
}
.table tbody tr { transition: transform 0.15s ease, box-shadow 0.15s ease; }
.table tbody tr:hover {
  transform: translateY(-2px);
  box-shadow: var(--ds-shadow-soft);
}
.table td {
  padding: 0.95rem 1rem;
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: 12px;
}
.table-actions { display: flex; gap: 0.45rem; }

/* BADGES */
.badge {
  padding: 6px 11px;
  border-radius: 999px;
  font-size: 0.86rem;
  font-weight: 800;
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
}
.badge-success { background: rgba(22, 163, 74, 0.14); color: #15803d; }
.badge-warning { background: rgba(245, 158, 11, 0.16); color: #b45309; }
.badge-info { background: rgba(91,107,255,0.15); color: #3745df; }
.priority-badge {
  padding: 7px 12px;
  border-radius: 11px;
  font-weight: 800;
}
.priority-urgent { background: rgba(239, 68, 68, 0.18); color: #b91c1c; }
.priority-high { background: rgba(245, 158, 11, 0.18); color: #b45309; }
.priority-medium { background: rgba(91,107,255,0.18); color: #3745df; }
.priority-low { background: rgba(22, 163, 74, 0.18); color: #15803d; }

/* FORMS */
.form-group { margin-bottom: 1rem; }
.input-text,
.input-select,
.input-search,
textarea,
select,
input[type="text"],
input[type="email"],
input[type="password"] {
  width: 100%;
  padding: 0.85rem 1rem;
  border-radius: 12px;
  border: 1px solid var(--ds-border);
  background: var(--ds-surface-2);
  color: var(--ds-text);
  transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}
.input-search { max-width: 260px; }
textarea { min-height: 120px; }
input:focus,
textarea:focus,
select:focus {
  outline: none;
  border-color: rgba(91, 107, 255, 0.35);
  background: #fff;
  box-shadow: 0 0 0 4px rgba(91, 107, 255, 0.12);
}

/* DETAIL BLOCKS */
.details-container { display: grid; gap: 1.3rem; }
.details-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 1rem;
}
.detail-item {
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: 12px;
  padding: 0.95rem 1rem;
  box-shadow: var(--ds-shadow-soft);
}
.detail-item label { display: block; color: var(--ds-muted); margin-bottom: 0.25rem; }
.detail-item p { margin: 0; color: var(--ds-text); font-weight: 700; }

/* TIMELINE */
.timeline {
  position: relative;
  margin-top: 1rem;
  border-left: 2px solid var(--ds-border);
  padding-left: 1.4rem;
  display: grid;
  gap: 1rem;
}
.timeline-item { position: relative; padding-left: 0.4rem; }
.timeline-dot {
  position: absolute;
  left: -1.1rem;
  top: 0.25rem;
  width: 12px;
  height: 12px;
  border-radius: 999px;
  background: linear-gradient(135deg, var(--ds-primary), var(--ds-accent));
  box-shadow: 0 0 0 4px rgba(91, 107, 255, 0.12);
}
.timeline-content {
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: 12px;
  padding: 0.9rem 1rem;
  box-shadow: var(--ds-shadow-soft);
}
.timestamp { color: var(--ds-muted); font-size: 0.9rem; }

/* CHAT */
.chat-container {
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: 16px;
  box-shadow: var(--ds-shadow-soft);
  padding: 1rem;
}
.message-group {
  display: grid;
  gap: 0.6rem;
  max-height: 420px;
  overflow-y: auto;
  padding: 0.5rem;
  background: var(--ds-surface-2);
  border-radius: 14px;
  border: 1px solid var(--ds-border);
}
.message {
  padding: 0.85rem 1rem;
  border-radius: 14px;
  max-width: 78%;
  box-shadow: var(--ds-shadow-soft);
}
.message-self {
  margin-left: auto;
  background: linear-gradient(135deg, var(--ds-primary), var(--ds-accent));
  color: #fff;
}
.message-other {
  margin-right: auto;
  background: #fff;
  border: 1px solid var(--ds-border);
}
.message-sender { font-weight: 700; margin-bottom: 0.15rem; color: inherit; }
.message-time { display: block; margin-top: 0.2rem; font-size: 0.85rem; color: rgba(15, 23, 42, 0.65); }
.message-form {
  margin-top: 0.85rem;
  display: flex;
  gap: 0.6rem;
  align-items: center;
}
.message-form input {
  flex: 1;
  border-radius: 999px;
  border: 1px solid var(--ds-border);
  padding: 0.8rem 1rem;
  background: var(--ds-surface);
}
.message-form input:focus { outline: none; border-color: rgba(91, 107, 255, 0.35); box-shadow: 0 0 0 4px rgba(91, 107, 255, 0.12); }

/* ALERTS */
.alert {
  padding: 0.9rem 1rem;
  border-radius: 12px;
  font-weight: 700;
  border: 1px solid transparent;
}
.alert-success { background: rgba(22, 163, 74, 0.12); color: #166534; border-color: rgba(22, 163, 74, 0.2); }
.alert-danger { background: rgba(239, 68, 68, 0.12); color: #991b1b; border-color: rgba(239, 68, 68, 0.2); }

/* MISC */
.simple-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  gap: 0.65rem;
}
.simple-list li {
  background: var(--ds-surface);
  border: 1px solid var(--ds-border);
  border-radius: 12px;
  padding: 1rem 1.1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: var(--ds-shadow-soft);
}
.grid-2-columns {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 1.1rem;
}
.mt-1 { margin-top: 1rem; }
.mt-2 { margin-top: 2rem; }

/* RESPONSIVE */
@media (max-width: 1024px) {
  .layout { flex-direction: column; }
  .sidebar {
    width: 100%;
    position: relative;
    min-height: auto;
    flex-direction: row;
    align-items: center;
    gap: 0.8rem;
    border-radius: 0 0 18px 18px;
  }
  .sidebar-nav { flex: 1; display: flex; flex-wrap: wrap; gap: 0.4rem; }
  .nav-item { flex: 1 1 150px; justify-content: center; }
  .main { padding: 1.2rem; }
}
@media (max-width: 640px) {
  .topbar, .top-bar { flex-direction: column; align-items: flex-start; }
  .stats-grid { grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); }
  .sidebar-nav { justify-content: center; }
  .section-header { flex-direction: column; align-items: flex-start; }
}
